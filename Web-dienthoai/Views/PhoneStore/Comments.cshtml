@*@using Web_dienthoai.Models
@model MatHangMua
@{
    ViewBag.Title = "Comments";
    Layout = "~/Views/Shared/_MasterLayout.cshtml";
}


<table class="table table-dark">
    <thead>
        <tr>
            <th>Mã sản phẩm</th>
            <th>Tên sản phẩm</th>
            <th>Hình ảnh</th>
            <th>Số lượng</th>
            <th>Đơn giá</th>
            <th>Thành tiền</th>

        </tr>
    </thead>
    <tbody>

        {
        <tr>
            <td>@Model.MaSP</td>
            <td>@Model.TenSP</td>

            <td><img src="~/Images/Phones/@Model.HinhSP" style="width:100px; height:30%" /></td>

            <td>
                <p>Số lượng:@Model.SoLuong</p>
               
            </td>
            <td>@String.Format("{0:0,0}", @Model.DonGia) đ</td>
            <td>@String.Format("{0:0,0}", @Model.ThanhTien()) đ</td>
        </tr>

    </tbody>


</table>
<div class="mb-3">

    <label for="exampleFormControlTextarea1" class="form-label">Bình luận</label>
    @using (Html.BeginForm("Comments", "PhoneStore"))
    {
        @Html.ValidationSummary(true)
        <textarea class="form-control" id="BinhLuan" rows="3"></textarea>
        <button type="submit" class="btn btn-primary">ĐĂNG NHẬP</button>
        @Html.ActionLink("Lưu", "Comments", "PhoneStore", new { @class = "btn btn-success" })
    }
</div>*@